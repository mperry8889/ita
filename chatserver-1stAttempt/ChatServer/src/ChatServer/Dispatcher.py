import socket

class Dispatcher(object):
    
    
    # wait for a connection.  when one arrives, create a user
    # around it and send it over the server 
    def __init__(self, port):
        s = socket()
        
        while 1:
            pass
            